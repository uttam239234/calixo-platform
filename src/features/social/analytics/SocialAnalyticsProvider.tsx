"use client";
import{createContext,useCallback,useContext,useEffect,useMemo,useState,type ReactNode}from"react";import{CheckCircle2,X}from"lucide-react";import{campaignSocialMetrics,dailySocialMetrics,platformMetrics,socialPosts}from"./mock-data";import type{AnalyticsOverview,DailySocialMetric,PlatformMetric,SocialAnalyticsFilters,SocialPostMetric}from"./types";
const STORAGE_KEY="calixo-social-analytics-filters-v1";const defaultFilters:SocialAnalyticsFilters={platform:"",date:"30d",campaign:"",postType:"",author:""};
interface AnalyticsContextValue{filters:SocialAnalyticsFilters;setFilters:(filters:SocialAnalyticsFilters)=>void;resetFilters:()=>void;posts:SocialPostMetric[];platforms:PlatformMetric[];series:DailySocialMetric[];overview:AnalyticsOverview;campaigns:typeof campaignSocialMetrics;aiVersion:number;refreshAi:()=>void;exportData:(format:"csv"|"excel"|"pdf")=>void;showToast:(message:string)=>void;}
const AnalyticsContext=createContext<AnalyticsContextValue|null>(null);
export function SocialAnalyticsProvider({children}:{children:ReactNode}){const[filters,setFilters]=useState(defaultFilters);const[hydrated,setHydrated]=useState(false);const[toast,setToast]=useState("");const[aiVersion,setAiVersion]=useState(1);useEffect(()=>{let active=true;queueMicrotask(()=>{if(!active)return;try{const stored=localStorage.getItem(STORAGE_KEY);if(stored)setFilters(JSON.parse(stored)as SocialAnalyticsFilters)}catch{localStorage.removeItem(STORAGE_KEY)}setHydrated(true)});return()=>{active=false}},[]);useEffect(()=>{if(hydrated)localStorage.setItem(STORAGE_KEY,JSON.stringify(filters))},[filters,hydrated]);useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(""),3200);return()=>clearTimeout(timer)},[toast]);const showToast=useCallback((message:string)=>setToast(message),[]);
const days=Number(filters.date.slice(0,-1));const latestDate=dailySocialMetrics.at(-1)?.date??"2026-01-01";const cutoffDate=new Date(`${latestDate}T00:00:00`);cutoffDate.setDate(cutoffDate.getDate()-(days-1));const cutoff=cutoffDate.toISOString().slice(0,10);const posts=useMemo(()=>socialPosts.filter(post=>post.date>=cutoff&&(!filters.platform||post.platform===filters.platform)&&(!filters.campaign||post.campaign===filters.campaign)&&(!filters.postType||post.postType===filters.postType)&&(!filters.author||post.author===filters.author)),[cutoff,filters]);const factor=(filters.platform?0.28:1)*(filters.campaign?0.62:1)*(filters.postType?0.78:1)*(filters.author?0.72:1);const series=useMemo(()=>dailySocialMetrics.slice(-days).map(item=>({...item,reach:Math.round(item.reach*factor),engagement:Math.round(item.engagement*factor),impressions:Math.round(item.impressions*factor),followers:Math.round(item.followers*(filters.platform?.length?0.18:1))})),[days,factor,filters.platform]);const platforms=useMemo(()=>platformMetrics.filter(item=>!filters.platform||item.platform===filters.platform).map(item=>({...item,reach:Math.round(item.reach*(filters.campaign?0.62:1)),posts:posts.filter(post=>post.platform===item.platform).length})),[filters.platform,filters.campaign,posts]);const overview=useMemo<AnalyticsOverview>(()=>{const followers=platforms.reduce((sum,item)=>sum+item.followers,0);const reach=posts.reduce((sum,item)=>sum+item.reach,0);const engagement=posts.reduce((sum,item)=>sum+item.engagement,0);const likes=posts.reduce((sum,item)=>sum+item.likes,0);const comments=posts.reduce((sum,item)=>sum+item.comments,0);const shares=posts.reduce((sum,item)=>sum+item.shares,0);const clicks=posts.reduce((sum,item)=>sum+item.clicks,0);return{followers,reach,impressions:Math.round(reach*1.68),engagement,likes,comments,shares,clicks,ctr:reach?Number((clicks/reach*100).toFixed(2)):0,growth:platforms.length?Number((platforms.reduce((sum,item)=>sum+item.growth,0)/platforms.length).toFixed(1)):0}},[platforms,posts]);
const resetFilters=useCallback(()=>setFilters(defaultFilters),[]);const refreshAi=useCallback(()=>{setAiVersion(value=>value+1);showToast("AI insights regenerated.")},[showToast]);const exportData=useCallback((format:"csv"|"excel"|"pdf")=>{const rows=[["Platform","Caption","Campaign","Reach","Engagement","Likes","Comments","Shares","CTR"],...posts.map(post=>[post.platform,post.caption,post.campaign,post.reach,post.engagement,post.likes,post.comments,post.shares,post.ctr])];const content=format==="pdf"?`CALIXO SOCIAL ANALYTICS REPORT\nGenerated ${new Date().toLocaleString()}\n\nFollowers: ${overview.followers}\nReach: ${overview.reach}\nEngagement: ${overview.engagement}\nCTR: ${overview.ctr}%\n\n${posts.map(post=>`${post.platform}: ${post.caption} — ${post.reach} reach`).join("\n")}`:rows.map(row=>row.map(value=>`"${String(value).replaceAll('"','""')}"`).join(",")).join("\n");const extension=format==="excel"?"xls":format;const blob=new Blob([content],{type:format==="pdf"?"application/pdf":format==="excel"?"application/vnd.ms-excel":"text/csv"});const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download=`calixo-social-analytics.${extension}`;link.click();URL.revokeObjectURL(url);showToast(`${format.toUpperCase()} report exported.`)},[posts,overview,showToast]);const value=useMemo(()=>({filters,setFilters,resetFilters,posts,platforms,series,overview,campaigns:campaignSocialMetrics,aiVersion,refreshAi,exportData,showToast}),[filters,resetFilters,posts,platforms,series,overview,aiVersion,refreshAi,exportData,showToast]);return<AnalyticsContext.Provider value={value}>{children}{toast&&<div role="status" className="fixed bottom-6 right-6 z-[80] flex items-center gap-3 rounded-2xl border border-emerald-500/30 bg-slate-900 px-4 py-3 text-sm text-white shadow-2xl"><CheckCircle2 size={18} className="text-emerald-400"/><span>{toast}</span><button onClick={()=>setToast("")} className="text-slate-500 hover:text-white"><X size={15}/></button></div>}</AnalyticsContext.Provider>}
export function useSocialAnalytics(){const context=useContext(AnalyticsContext);if(!context)throw new Error("useSocialAnalytics must be used within SocialAnalyticsProvider");return context}
