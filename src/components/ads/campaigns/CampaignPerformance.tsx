import { Activity, Banknote, Eye, MousePointerClick, Target, TrendingUp } from "lucide-react";
import type { Campaign } from "@/features/ads/types";
const fmt = (n: number) => n >= 1000000 ? `${(n / 1000000).toFixed(1)}M` : n >= 1000 ? `${(n / 1000).toFixed(1)}K` : n.toLocaleString();
export function CampaignPerformance({ campaign }: { campaign: Campaign }) { const metrics = [["Spend", `$${fmt(campaign.spend)}`, Banknote], ["Revenue", `$${fmt(campaign.revenue)}`, TrendingUp], ["Conversions", fmt(campaign.conversions), Target], ["CTR", `${campaign.ctr.toFixed(2)}%`, MousePointerClick], ["CPA", `$${campaign.cpa.toFixed(2)}`, Activity], ["ROAS", `${campaign.roas.toFixed(1)}x`, TrendingUp], ["Clicks", fmt(campaign.clicks), MousePointerClick], ["Impressions", fmt(campaign.impressions), Eye], ["Quality score", `${campaign.qualityScore}/10`, Target]]; return <section id="performance"><h2 className="mb-3 text-lg font-semibold text-white">Performance</h2><div className="grid gap-3 sm:grid-cols-3 xl:grid-cols-5">{metrics.map(([label, value, Icon]) => <div key={label as string} className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><div className="flex items-center justify-between text-xs text-slate-500"><span>{label as string}</span><Icon size={15} className="text-cyan-300" /></div><p className="mt-2 text-xl font-semibold text-white">{value as string}</p></div>)}</div><div className="mt-4 rounded-3xl border border-slate-800 bg-slate-900/70 p-5"><div className="flex h-36 items-end gap-2">{[38,48,42,59,54,68,63,76,70,86,82,94,89,100].map((v,i) => <div key={i} className="flex-1 rounded-t bg-gradient-to-t from-cyan-600/40 to-cyan-300/90 transition hover:to-white" style={{ height: `${v}%` }} />)}</div><div className="mt-3 flex justify-between text-[10px] text-slate-600"><span>Jul 1</span><span>Jul 15</span><span>Jul 31</span></div></div></section>; }
