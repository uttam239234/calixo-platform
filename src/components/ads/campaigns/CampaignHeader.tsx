import Link from "next/link";
import { ArrowLeft, Copy, Pause, Pencil, Play } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { Campaign, Platform } from "@/features/ads/types";
import { campaignStatusStyle } from "./CampaignRow";
export function CampaignHeader({ campaign, platform, onEdit, onAction }: { campaign: Campaign; platform: Platform; onEdit: () => void; onAction: (action: "Pause" | "Resume" | "Duplicate") => void }) { return <div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><Link href="/dashboard/ads/campaigns" className="mb-4 inline-flex items-center gap-2 text-xs text-slate-400 hover:text-cyan-300"><ArrowLeft size={14} /> All campaigns</Link><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl font-bold" style={{ backgroundColor: `${platform.color}20`, color: platform.color }}>{platform.shortName}</span><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-2xl font-semibold text-white sm:text-3xl">{campaign.name}</h1><span className={`rounded-full px-2.5 py-1 text-xs ${campaignStatusStyle[campaign.status]}`}>{campaign.status}</span></div><p className="mt-1 text-sm text-slate-400">{platform.name} · {campaign.objective} · Owned by {campaign.owner}</p></div></div></div><div className="flex flex-wrap gap-2"><Button onClick={() => onAction("Duplicate")} variant="outline" className="border-slate-700 bg-slate-900 text-slate-300"><Copy /> Duplicate</Button><Button onClick={() => onAction(campaign.status === "Paused" ? "Resume" : "Pause")} variant="outline" className="border-slate-700 bg-slate-900 text-slate-300">{campaign.status === "Paused" ? <Play /> : <Pause />}{campaign.status === "Paused" ? "Resume" : "Pause"}</Button><Button onClick={onEdit} className="bg-cyan-400 text-slate-950 hover:bg-cyan-300"><Pencil /> Edit campaign</Button></div></div>; }
