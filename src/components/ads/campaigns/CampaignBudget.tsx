import type { Campaign } from "@/features/ads/types";
export function CampaignBudget({ campaign }: { campaign: Campaign }) { const pct = Math.min(100, Math.round(campaign.spend / campaign.budget * 100)); return <section id="budget" className="rounded-3xl border border-slate-800 bg-slate-900/70 p-5"><h2 className="text-lg font-semibold text-white">Budget</h2><div className="mt-5 flex items-end justify-between"><div><p className="text-xs text-slate-500">Total allocation</p><p className="mt-1 text-2xl font-semibold text-white">${campaign.budget.toLocaleString()}</p></div><span className="text-sm text-cyan-300">{pct}% spent</span></div><div className="mt-4 h-2 rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-cyan-500 to-blue-500" style={{ width: `${pct}%` }} /></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-slate-950/50 p-3"><span className="text-slate-500">Spent</span><p className="mt-1 font-medium text-white">${campaign.spend.toLocaleString()}</p></div><div className="rounded-xl bg-slate-950/50 p-3"><span className="text-slate-500">Remaining</span><p className="mt-1 font-medium text-white">${Math.max(0, campaign.budget-campaign.spend).toLocaleString()}</p></div></div></section>; }
