"use client";
import { CheckCircle2, RefreshCw, TriangleAlert, Unplug } from "lucide-react";
import Card from "@/components/dashboard/common/Card";
import { useSocial } from "@/features/social/SocialProvider";
const compact=(value:number)=>new Intl.NumberFormat("en-US",{notation:"compact",maximumFractionDigits:1}).format(value);
export function PlatformOverview(){const{accounts,syncAccount,engagementInsight}=useSocial();return <section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-semibold text-foreground">Platform overview</h2><span className="text-xs text-muted-foreground">Live account health</span></div><p className="mb-3 text-xs text-muted-foreground">{engagementInsight}</p><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-6">{accounts.map(account=>{const StatusIcon=account.status==="Connected"?CheckCircle2:account.status==="Disconnected"?Unplug:TriangleAlert;return <Card key={account.id} className="p-4"><div className="flex items-start justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl text-sm font-bold" style={{backgroundColor:`${account.color}20`,color:account.color}}>{account.shortName}</span><StatusIcon size={16} className={account.status==="Connected"?"text-success":"text-warning"}/></div><p className="mt-4 text-sm font-semibold text-foreground">{account.platform}</p><p className="mt-1 text-2xl font-semibold text-foreground">{compact(account.followers)}</p><div className="mt-4 flex items-center justify-between text-xs"><span className="text-muted-foreground">{account.engagementRate}% engagement</span><button onClick={()=>syncAccount(account.id)} aria-label={`Sync ${account.platform}`} className="text-muted-foreground hover:text-primary"><RefreshCw size={14}/></button></div></Card>})}</div></section>}
