"use client";
import{Cell,Pie,PieChart,ResponsiveContainer,Tooltip}from"recharts";import{useSocialAnalytics}from"@/features/social/analytics/SocialAnalyticsProvider";
const colors=["var(--primary)","#8b5cf6","#34d399","#f59e0b","#f472b6"];
export function ContentPerformance(){const{posts}=useSocialAnalytics();const data=["Carousel","Video","Image","Text","Article"].map(name=>({name,value:posts.filter(item=>item.postType===name).reduce((sum,item)=>sum+item.engagement,0)}));return<section className="rounded-3xl border border-border bg-card/70 p-5"><h2 className="text-lg font-semibold text-foreground">Content performance</h2><p className="mt-1 text-xs text-muted-foreground">Engagement contribution by post type</p><div className="mt-3 grid items-center sm:grid-cols-[1fr_150px]"><div className="h-56"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" innerRadius={52} outerRadius={82} paddingAngle={3}>{data.map((item,index)=><Cell key={item.name} fill={colors[index]}/>)}</Pie><Tooltip contentStyle={{backgroundColor:"#0f172a",borderColor:"#334155",borderRadius:12}}/></PieChart></ResponsiveContainer></div><div className="space-y-2">{data.map((item,index)=><div key={item.name} className="flex items-center justify-between text-xs"><span className="flex items-center gap-2 text-muted-foreground"><span className="h-2 w-2 rounded-full" style={{backgroundColor:colors[index]}}/>{item.name}</span><span className="text-foreground">{item.value.toLocaleString()}</span></div>)}</div></div></section>}
