"use client";
import Link from"next/link";import{ArrowLeft}from"lucide-react";import{useSocialAnalytics}from"@/features/social/analytics/SocialAnalyticsProvider";import{ExportToolbar}from"./ExportToolbar";
const cls="h-9 rounded-xl border border-border bg-surface px-3 text-xs text-foreground outline-none";
export function AnalyticsHeader(){const{filters,setFilters,resetFilters,posts}=useSocialAnalytics();const campaigns=[...new Set(posts.map(item=>item.campaign))];const update=(key:keyof typeof filters,value:string)=>setFilters({...filters,[key]:value});return<div className="space-y-5"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><Link href="/dashboard/social" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-primary"><ArrowLeft size={14}/>Social Media Manager</Link><p className="mt-4 text-xs font-semibold uppercase tracking-[0.22em] text-primary">Social intelligence</p><h1 className="mt-2 text-3xl font-semibold text-foreground">Social Analytics</h1><p className="mt-2 text-sm text-muted-foreground">Understand content, audience, and platform performance from one premium workspace.</p></div><ExportToolbar/></div><div className="flex gap-2 overflow-x-auto rounded-2xl border border-border bg-card/55 p-3"><select className={cls} value={filters.platform} onChange={event=>update("platform",event.target.value)}><option value="">All platforms</option>{["Facebook","Instagram","LinkedIn","X","TikTok","Pinterest","Threads","YouTube"].map(item=><option key={item}>{item}</option>)}</select><select className={cls} value={filters.date} onChange={event=>update("date",event.target.value)}><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option><option value="90d">Last 90 days</option></select><select className={cls} value={filters.campaign} onChange={event=>update("campaign",event.target.value)}><option value="">All campaigns</option>{campaigns.map(item=><option key={item}>{item}</option>)}</select><select className={cls} value={filters.postType} onChange={event=>update("postType",event.target.value)}><option value="">All post types</option>{["Carousel","Video","Image","Text","Article"].map(item=><option key={item}>{item}</option>)}</select><select className={cls} value={filters.author} onChange={event=>update("author",event.target.value)}><option value="">All authors</option>{["Aarav Mehta","Maya Chen","Sofia Rossi","Noah Williams"].map(item=><option key={item}>{item}</option>)}</select>{Object.entries(filters).some(([key,value])=>key!=="date"&&Boolean(value))&&<button onClick={resetFilters} className="shrink-0 px-2 text-xs text-primary">Reset</button>}</div></div>}
