"use client";
import { ArrowRight, Bot, Cable, CalendarPlus, FilePlus2 } from "lucide-react";
import { useRouter } from "next/navigation";
import { useSocial } from "@/features/social/SocialProvider";
export function QuickActions(){const router=useRouter();const{accounts,generateAiPost,toggleAccount,showToast}=useSocial();const connect=()=>{const account=accounts.find(x=>x.status!=="Connected");if(account)toggleAccount(account.id);else showToast("All social accounts are already connected.");};const actions=[{title:"Create post",description:"Start a new social draft",icon:FilePlus2,accent:"text-primary bg-primary/10",action:()=>router.push("/dashboard/social/compose")},{title:"Schedule post",description:"Add content to your calendar",icon:CalendarPlus,accent:"text-ai bg-ai/10",action:()=>router.push("/dashboard/social/calendar")},{title:"Connect account",description:"Add another social channel",icon:Cable,accent:"text-info bg-info/10",action:connect},{title:"Generate AI post",description:"Draft platform-ready content",icon:Bot,accent:"text-ai bg-ai/10",action:generateAiPost}];return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{actions.map(({title,description,icon:Icon,accent,action})=><button key={title} onClick={action} className="group flex items-center gap-4 rounded-2xl border border-border bg-surface/65 p-4 text-left backdrop-blur-xl transition duration-300 hover:-translate-y-0.5 hover:border-primary/30 hover:bg-surface"><span className={`flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl ${accent}`}><Icon size={20}/></span><span className="min-w-0"><span className="block text-sm font-semibold text-foreground">{title}</span><span className="mt-1 block truncate text-xs text-muted-foreground">{description}</span></span><ArrowRight size={16} className="ml-auto text-muted-foreground transition group-hover:translate-x-1 group-hover:text-primary"/></button>)}</div>}
