"use client";
import Link from"next/link";import{ArrowLeft,Plus}from"lucide-react";import{Button}from"@/components/ui/button";import{useCalendar}from"@/features/social/calendar/CalendarProvider";
export function CalendarHeader(){const{openCreate}=useCalendar();return<div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/dashboard/social" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-primary"><ArrowLeft size={14}/>Social Media Manager</Link><p className="mt-4 text-xs font-semibold uppercase tracking-[0.22em] text-primary">Content operations</p><h1 className="mt-2 text-3xl font-semibold text-foreground">Content Calendar</h1><p className="mt-2 text-sm text-muted-foreground">Plan, coordinate, and publish every social post on time.</p></div><Button onClick={()=>openCreate()} className="h-10 bg-primary px-4 text-primary-foreground hover:bg-primary/90"><Plus/>Schedule post</Button></div>}
