"use client";
import{AlertTriangle,LoaderCircle,RotateCcw}from"lucide-react";import{useCalendar}from"@/features/social/calendar/CalendarProvider";
export function PublishingQueue(){const{events,openEdit,publishNow}=useCalendar();const publishing=events.filter(event=>event.status==="Publishing");const failed=events.filter(event=>event.status==="Failed");return<section><h2 className="text-sm font-semibold text-white">Publishing queue</h2><div className="mt-3 space-y-2">{publishing.map(event=><button key={event.id} onClick={()=>openEdit(event.id)} className="flex w-full items-center gap-2 rounded-xl bg-cyan-500/5 p-3 text-left"><LoaderCircle size={14} className="animate-spin text-cyan-300"/><span className="truncate text-xs text-slate-300">{event.title}</span></button>)}{failed.map(event=><div key={event.id} className="flex items-center gap-2 rounded-xl bg-rose-500/5 p-3"><AlertTriangle size={14} className="text-rose-300"/><button onClick={()=>openEdit(event.id)} className="min-w-0 flex-1 truncate text-left text-xs text-rose-200">{event.title}</button><button onClick={()=>publishNow(event.id)} aria-label={`Retry ${event.title}`} className="text-rose-300 hover:text-white"><RotateCcw size={13}/></button></div>)}{!publishing.length&&!failed.length&&<p className="py-4 text-center text-xs text-slate-600">Queue is clear.</p>}</div></section>}
