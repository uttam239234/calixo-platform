"use client";
import{ChevronLeft,ChevronRight,Search,SlidersHorizontal}from"lucide-react";import{Button}from"@/components/ui/button";import{useCalendar}from"@/features/social/calendar/CalendarProvider";import{CalendarFilters}from"./CalendarFilters";import{CalendarViewSwitcher}from"./CalendarViewSwitcher";import{useState}from"react";
export function CalendarToolbar(){const{events,view,setView,query,setQuery,filters,setFilters,navigate}=useCalendar();const[showFilters,setShowFilters]=useState(false);return<div className="sticky top-0 z-20 space-y-3 rounded-2xl border border-slate-800 bg-slate-950/90 p-3 shadow-xl backdrop-blur-xl"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="flex items-center gap-1"><Button variant="outline" size="icon" onClick={()=>navigate(-1)} className="border-slate-700 bg-slate-900 text-slate-300"><ChevronLeft/></Button><Button variant="outline" onClick={()=>navigate(0)} className="border-slate-700 bg-slate-900 text-slate-300">Today</Button><Button variant="outline" size="icon" onClick={()=>navigate(1)} className="border-slate-700 bg-slate-900 text-slate-300"><ChevronRight/></Button></div><label className="flex h-9 flex-1 items-center gap-2 rounded-xl border border-slate-700 bg-slate-900 px-3 text-slate-400"><Search size={15}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search title, campaign, platform or tag…" className="w-full bg-transparent text-sm text-white outline-none placeholder:text-slate-600"/></label><Button variant="outline" onClick={()=>setShowFilters(value=>!value)} className="border-slate-700 bg-slate-900 text-slate-300"><SlidersHorizontal/>Filters</Button><CalendarViewSwitcher view={view} onChange={setView}/></div>{showFilters&&<CalendarFilters filters={filters} events={events} onChange={setFilters}/>}</div>}
