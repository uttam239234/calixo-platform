"use client";
import{ChevronLeft,ChevronRight,Search,SlidersHorizontal}from"lucide-react";import{Button}from"@/components/ui/button";import{useCalendar}from"@/features/social/calendar/CalendarProvider";import{CalendarFilters}from"./CalendarFilters";import{CalendarViewSwitcher}from"./CalendarViewSwitcher";import{useState}from"react";
export function CalendarToolbar(){const{events,view,setView,query,setQuery,filters,setFilters,navigate}=useCalendar();const[showFilters,setShowFilters]=useState(false);return<div className="sticky top-0 z-20 space-y-3 rounded-2xl border border-border bg-card p-3 shadow-xl backdrop-blur-xl"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="flex items-center gap-1"><Button variant="outline" size="icon" onClick={()=>navigate(-1)} className="border-border bg-surface text-foreground"><ChevronLeft/></Button><Button variant="outline" onClick={()=>navigate(0)} className="border-border bg-surface text-foreground">Today</Button><Button variant="outline" size="icon" onClick={()=>navigate(1)} className="border-border bg-surface text-foreground"><ChevronRight/></Button></div><label className="flex h-9 flex-1 items-center gap-2 rounded-xl border border-border bg-surface px-3 text-muted-foreground"><Search size={15}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search title, campaign, platform or tag…" className="w-full bg-transparent text-sm text-foreground outline-none placeholder:text-muted-foreground"/></label><Button variant="outline" onClick={()=>setShowFilters(value=>!value)} className="border-border bg-surface text-foreground"><SlidersHorizontal/>Filters</Button><CalendarViewSwitcher view={view} onChange={setView}/></div>{showFilters&&<CalendarFilters filters={filters} events={events} onChange={setFilters}/>}</div>}
