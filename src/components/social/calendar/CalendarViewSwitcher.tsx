"use client";
import{CalendarDays,Columns3,List,Rows3}from"lucide-react";import type{CalendarView}from"@/features/social/calendar/types";
export function CalendarViewSwitcher({view,onChange}:{view:CalendarView;onChange:(view:CalendarView)=>void}){const options=[{value:"day"as const,label:"Day",icon:Rows3},{value:"week"as const,label:"Week",icon:Columns3},{value:"month"as const,label:"Month",icon:CalendarDays},{value:"agenda"as const,label:"Agenda",icon:List}];return<div className="flex rounded-xl border border-border bg-surface p-1">{options.map(({value,label,icon:Icon})=><button key={value} onClick={()=>onChange(value)} className={`flex items-center gap-1.5 rounded-lg px-3 py-2 text-xs ${view===value?"bg-primary/15 text-primary":"text-muted-foreground hover:text-foreground"}`}><Icon size={14}/><span className="hidden sm:inline">{label}</span></button>)}</div>}
