import type{InboxMessage}from"@/features/social/inbox/types";import{MediaAttachment}from"./MediaAttachment";
export function MessageBubble({message}:{message:InboxMessage}){const agent=message.sender==="agent";return<div className={`flex ${agent?"justify-end":"justify-start"}`}><div className={`max-w-[82%] rounded-2xl px-4 py-3 ${agent?"rounded-br-md bg-primary/15 text-foreground":"rounded-bl-md border border-border bg-surface text-foreground"}`}><div className="mb-1 flex items-center gap-2"><span className="text-[10px] font-medium text-muted-foreground">{message.author}</span><span className="text-[9px] text-muted-foreground">{new Date(message.timestamp).toLocaleString()}</span></div>{message.body&&<p className="whitespace-pre-wrap text-sm leading-6">{message.body}</p>}{message.attachments.length>0&&<div className="mt-2 flex flex-wrap gap-2">{message.attachments.map(item=><MediaAttachment key={item.id} attachment={item}/>)}</div>}</div></div>}
