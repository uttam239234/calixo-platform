import type{InboxMessage}from"@/features/social/inbox/types";import{MediaAttachment}from"./MediaAttachment";
export function MessageBubble({message}:{message:InboxMessage}){const agent=message.sender==="agent";return<div className={`flex ${agent?"justify-end":"justify-start"}`}><div className={`max-w-[82%] rounded-2xl px-4 py-3 ${agent?"rounded-br-md bg-cyan-500/15 text-cyan-50":"rounded-bl-md border border-slate-800 bg-slate-900 text-slate-200"}`}><div className="mb-1 flex items-center gap-2"><span className="text-[10px] font-medium text-slate-400">{message.author}</span><span className="text-[9px] text-slate-600">{new Date(message.timestamp).toLocaleString()}</span></div>{message.body&&<p className="whitespace-pre-wrap text-sm leading-6">{message.body}</p>}{message.attachments.length>0&&<div className="mt-2 flex flex-wrap gap-2">{message.attachments.map(item=><MediaAttachment key={item.id} attachment={item}/>)}</div>}</div></div>}
