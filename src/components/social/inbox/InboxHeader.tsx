"use client";
import Link from"next/link";import{ArrowLeft,CheckCheck}from"lucide-react";import{Button}from"@/components/ui/button";import{useInbox}from"@/features/social/inbox/InboxProvider";
export function InboxHeader(){const{conversations,markAllRead}=useInbox();const unread=conversations.reduce((sum,item)=>sum+item.unread,0);return<div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/dashboard/social" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-primary"><ArrowLeft size={14}/>Social Media Manager</Link><p className="mt-4 text-xs font-semibold uppercase tracking-[0.22em] text-primary">Customer conversations</p><h1 className="mt-2 text-3xl font-semibold text-foreground">Unified Social Inbox</h1><p className="mt-2 text-sm text-muted-foreground">Respond, assign, and resolve every social conversation in one place.</p></div><Button onClick={markAllRead} disabled={!unread} variant="outline" className="h-10 border-border bg-card text-foreground"><CheckCheck/>Mark all read ({unread})</Button></div>}
