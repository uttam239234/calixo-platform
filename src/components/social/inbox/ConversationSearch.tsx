"use client";
import{Search,X}from"lucide-react";
export function ConversationSearch({value,onChange}:{value:string;onChange:(value:string)=>void}){return<label className="flex h-10 items-center gap-2 rounded-xl border border-slate-700 bg-slate-950/60 px-3 text-slate-400"><Search size={15}/><input value={value} onChange={event=>onChange(event.target.value)} placeholder="Search customers, messages, platforms or tags…" className="w-full bg-transparent text-sm text-white outline-none placeholder:text-slate-600"/>{value&&<button onClick={()=>onChange("")} aria-label="Clear search"><X size={14}/></button>}</label>}
