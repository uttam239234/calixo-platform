"use client";
import{Search,X}from"lucide-react";
export function ConversationSearch({value,onChange}:{value:string;onChange:(value:string)=>void}){return<label className="flex h-10 items-center gap-2 rounded-xl border border-border bg-surface/60 px-3 text-muted-foreground"><Search size={15}/><input value={value} onChange={event=>onChange(event.target.value)} placeholder="Search customers, messages, platforms or tags…" className="w-full bg-transparent text-sm text-foreground outline-none placeholder:text-muted-foreground"/>{value&&<button onClick={()=>onChange("")} aria-label="Clear search"><X size={14}/></button>}</label>}
