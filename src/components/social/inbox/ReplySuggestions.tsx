"use client";
const suggestions=["Thanks for reaching out — I’m happy to help.","Let me check that for you and share the next step.","Could you send a screenshot so we can investigate?","I’ve escalated this to our support team."];
export function ReplySuggestions({onSelect}:{onSelect:(value:string)=>void}){return<div className="flex gap-2 overflow-x-auto pb-1">{suggestions.map(item=><button key={item} onClick={()=>onSelect(item)} className="shrink-0 rounded-xl border border-border bg-surface/40 px-3 py-2 text-xs text-muted-foreground hover:border-primary/30 hover:text-primary">{item}</button>)}</div>}
