"use client";
import{AtSign,Flag}from"lucide-react";import{formatRelativeTime}from"@/features/social/inbox/inbox-utils";import type{Conversation}from"@/features/social/inbox/types";import{PlatformBadge}from"./PlatformBadge";import{SentimentIndicator}from"./SentimentIndicator";
export function ConversationCard({conversation,active,onClick}:{conversation:Conversation;active:boolean;onClick:()=>void}){const last=conversation.messages.at(-1);return<button onClick={onClick} className={`w-full border-b border-slate-800/70 p-4 text-left transition ${active?"bg-cyan-500/[0.08] shadow-[inset_3px_0_0_#22d3ee]":"hover:bg-slate-800/45"}`}><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-gradient-to-br from-slate-700 to-slate-800 text-xs font-semibold text-white">{conversation.customer.avatar}</span><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="truncate text-sm font-semibold text-slate-200">{conversation.customer.name}</span><PlatformBadge platform={conversation.platform} compact/><span className="ml-auto shrink-0 text-[10px] text-slate-600">{formatRelativeTime(conversation.updatedAt)}</span></div><p className={`mt-1 truncate text-xs ${conversation.unread?"font-medium text-slate-300":"text-slate-500"}`}>{last?.body||"No messages"}</p><div className="mt-2 flex items-center gap-2"><SentimentIndicator sentiment={conversation.sentiment}/>{["High","Urgent"].includes(conversation.priority)&&<span className="flex items-center gap-1 text-[10px] text-rose-300"><Flag size={10}/>{conversation.priority}</span>}{conversation.mentions>0&&<span className="flex items-center gap-1 text-[10px] text-violet-300"><AtSign size={10}/>{conversation.mentions}</span>}{conversation.unread>0&&<span className="ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-cyan-400 px-1 text-[10px] font-semibold text-slate-950">{conversation.unread}</span>}</div></div></div></button>}
