"use client";
import{AtSign,Flag}from"lucide-react";import{formatRelativeTime}from"@/features/social/inbox/inbox-utils";import type{Conversation}from"@/features/social/inbox/types";import{PlatformBadge}from"./PlatformBadge";import{SentimentIndicator}from"./SentimentIndicator";
export function ConversationCard({conversation,active,onClick}:{conversation:Conversation;active:boolean;onClick:()=>void}){const last=conversation.messages.at(-1);return<button onClick={onClick} className={`w-full border-b border-border/70 p-4 text-left transition ${active?"bg-primary/[0.08] shadow-[inset_3px_0_0_var(--primary)]":"hover:bg-surface/45"}`}><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-gradient-to-br from-muted to-surface text-xs font-semibold text-foreground">{conversation.customer.avatar}</span><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="truncate text-sm font-semibold text-foreground">{conversation.customer.name}</span><PlatformBadge platform={conversation.platform} compact/><span className="ml-auto shrink-0 text-[10px] text-muted-foreground">{formatRelativeTime(conversation.updatedAt)}</span></div><p className={`mt-1 truncate text-xs ${conversation.unread?"font-medium text-foreground":"text-muted-foreground"}`}>{last?.body||"No messages"}</p><div className="mt-2 flex items-center gap-2"><SentimentIndicator sentiment={conversation.sentiment}/>{["High","Urgent"].includes(conversation.priority)&&<span className="flex items-center gap-1 text-[10px] text-destructive"><Flag size={10}/>{conversation.priority}</span>}{conversation.mentions>0&&<span className="flex items-center gap-1 text-[10px] text-ai"><AtSign size={10}/>{conversation.mentions}</span>}{conversation.unread>0&&<span className="ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-primary px-1 text-[10px] font-semibold text-primary-foreground">{conversation.unread}</span>}</div></div></div></button>}
