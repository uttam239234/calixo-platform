"use client";
import { Copy, FileText, FolderOpen, Trash2 } from "lucide-react";
import type { ComposerDraft } from "@/features/social/composer-types";
export function DraftManager({drafts,hydrated,onLoad,onDuplicate,onDelete}:{drafts:ComposerDraft[];hydrated:boolean;onLoad:(draft:ComposerDraft)=>void;onDuplicate:(draft:ComposerDraft)=>void;onDelete:(id:string)=>void}){return <section><div className="mb-3 flex items-center justify-between"><div><h2 className="text-sm font-semibold text-white">Draft manager</h2><p className="mt-1 text-xs text-slate-500">Saved locally in this browser.</p></div><span className="text-xs text-cyan-300">{drafts.length} saved</span></div>{!hydrated?<div className="h-20 animate-pulse rounded-2xl bg-slate-950/40"/>:drafts.length?<div className="max-h-60 space-y-2 overflow-auto">{drafts.map(draft=><div key={draft.id} className="flex items-center gap-3 rounded-2xl border border-slate-800 bg-slate-950/35 p-3"><FileText size={16} className="shrink-0 text-cyan-300"/><div className="min-w-0 flex-1"><p className="truncate text-xs font-medium text-slate-200">{draft.title}</p><p className="mt-0.5 text-[10px] text-slate-600">{draft.platforms.length} platforms · {new Date(draft.updatedAt).toLocaleString()}</p></div><button onClick={()=>onLoad(draft)} aria-label={`Load ${draft.title}`} className="text-slate-500 hover:text-cyan-300"><FolderOpen size={14}/></button><button onClick={()=>onDuplicate(draft)} aria-label={`Duplicate ${draft.title}`} className="text-slate-500 hover:text-white"><Copy size={14}/></button><button onClick={()=>onDelete(draft.id)} aria-label={`Delete ${draft.title}`} className="text-slate-500 hover:text-rose-300"><Trash2 size={14}/></button></div>)}</div>:<div className="rounded-2xl border border-dashed border-slate-800 py-7 text-center text-xs text-slate-600">No saved drafts yet.</div>}</section>}
