"use client";
import Link from "next/link";
import { ArrowLeft, CalendarClock, Copy, Save, Send, Trash2 } from "lucide-react";
import Card from "@/components/dashboard/common/Card";
import { Button } from "@/components/ui/button";
import { useSocial } from "@/features/social/SocialProvider";
import { usePostComposer } from "@/features/social/usePostComposer";
import type { SocialPlatform } from "@/features/social/types";
import { PlatformSelector } from "./PlatformSelector";import { ContentEditor } from "./ContentEditor";import { MediaUploader } from "./MediaUploader";import { HashtagSuggestions } from "./HashtagSuggestions";import { AiAssistantPanel } from "./AiAssistantPanel";import { PostPreview } from "./PostPreview";import { PublishingOptions } from "./PublishingOptions";import { AudienceSelector } from "./AudienceSelector";import { DraftManager } from "./DraftManager";
export function SocialComposer(){const composer=usePostComposer();const{draft}=composer;const{publishComposedPost,showToast}=useSocial();const addText=(value:string)=>composer.update("content",`${draft.content}${draft.content&&!draft.content.endsWith(" ")?" ":""}${value}`);const publish=(scheduled:boolean)=>{if(!composer.canPublish)return;const status=scheduled?"Scheduled"as const:"Published"as const;const date=scheduled?`${draft.scheduledDate} ${draft.scheduledTime} (${draft.timezone})`:"Just now";publishComposedPost(draft.platforms as SocialPlatform[],draft.content,status,date);showToast(scheduled?"Post scheduled across selected platforms.":"Post published across selected platforms.");composer.discard()};const save=()=>{composer.saveDraft();showToast("Draft saved locally.")};const duplicate=()=>{const saved=composer.saveDraft();composer.duplicateDraft(saved);showToast("Draft duplicated.")};return <div className="space-y-6 pb-28"><div><Link href="/dashboard/social" className="inline-flex items-center gap-2 text-xs text-slate-400 hover:text-cyan-300"><ArrowLeft size={14}/>Social Media Manager</Link><div className="mt-4"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-cyan-300">Multi-platform composer</p><h1 className="mt-2 text-3xl font-semibold text-white">Create social post</h1><p className="mt-2 text-sm text-slate-400">Compose, adapt, preview, and publish everywhere from one workspace.</p></div></div><Card className="p-5" hover={false}><PlatformSelector selected={draft.platforms} onToggle={composer.togglePlatform}/></Card><div className="grid gap-6 xl:grid-cols-[1.15fr_0.85fr]"><div className="space-y-6"><Card className="space-y-6 p-5" hover={false}><ContentEditor content={draft.content} platforms={draft.platforms} onChange={value=>composer.update("content",value)}/><HashtagSuggestions onAdd={addText}/><MediaUploader media={draft.media} onAdd={composer.addMedia} onRemove={composer.removeMedia} onMove={composer.moveMedia}/></Card><AiAssistantPanel busy={composer.aiBusy} onRun={composer.runAi}/><Card className="p-5" hover={false}><DraftManager drafts={composer.savedDrafts} hydrated={composer.hydrated} onLoad={item=>{composer.loadDraft(item);showToast("Draft restored.")}} onDuplicate={item=>{composer.duplicateDraft(item);showToast("Draft duplicated.")}} onDelete={id=>{composer.deleteDraft(id);showToast("Draft deleted.")}}/></Card></div><div className="space-y-6"><Card className="p-5" hover={false}><PostPreview draft={draft} onPlatformChange={value=>composer.update("previewPlatform",value)}/></Card><Card className="space-y-6 p-5" hover={false}><AudienceSelector value={draft.audience} customAudience={draft.customAudience} onChange={value=>composer.update("audience",value)} onCustomChange={value=>composer.update("customAudience",value)}/><PublishingOptions draft={draft} onUpdate={composer.update}/></Card>{composer.validationMessages.length>0&&<div className="rounded-2xl border border-amber-500/20 bg-amber-500/5 p-4"><p className="text-xs font-semibold text-amber-300">Before publishing</p><ul className="mt-2 space-y-1 text-xs text-slate-400">{composer.validationMessages.map(message=><li key={message}>• {message}</li>)}</ul></div>}</div></div><div className="fixed bottom-0 left-0 right-0 z-30 border-t border-slate-800 bg-slate-950/90 p-3 backdrop-blur-xl md:left-72"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-end gap-2"><Button variant="ghost" onClick={()=>{if(window.confirm("Discard this unfinished post?")){composer.discard();showToast("Composer cleared.")}}} className="text-slate-400"><Trash2/>Discard</Button><Button variant="outline" onClick={duplicate} className="border-slate-700 bg-slate-900 text-slate-300"><Copy/>Duplicate</Button><Button variant="outline" onClick={save} className="border-slate-700 bg-slate-900 text-slate-300"><Save/>Save draft</Button><Button disabled={!composer.canPublish} onClick={()=>publish(true)} variant="outline" className="border-cyan-500/30 bg-cyan-500/5 text-cyan-300"><CalendarClock/>Schedule</Button><Button disabled={!composer.canPublish} onClick={()=>publish(false)} className="bg-cyan-400 px-5 text-slate-950 hover:bg-cyan-300"><Send/>Publish</Button></div></div></div>}
