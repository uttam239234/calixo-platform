"use client";
import { Check } from "lucide-react";
import { composerPlatforms } from "@/features/social/composer-config";
import type { ComposerPlatform } from "@/features/social/composer-types";
export function PlatformSelector({selected,onToggle}:{selected:ComposerPlatform[];onToggle:(platform:ComposerPlatform)=>void}){return <section><div className="mb-3 flex items-center justify-between"><div><h2 className="text-sm font-semibold text-white">Choose platforms</h2><p className="mt-1 text-xs text-slate-500">Select one or more publishing destinations.</p></div><span className="text-xs text-cyan-300">{selected.length} selected</span></div><div className="grid grid-cols-2 gap-2 sm:grid-cols-4">{composerPlatforms.map(platform=>{const active=selected.includes(platform.name);return <button key={platform.name} onClick={()=>onToggle(platform.name)} className={`relative flex items-center gap-2 rounded-xl border p-3 text-left transition ${active?"border-cyan-500/50 bg-cyan-500/10":"border-slate-800 bg-slate-950/40 hover:border-slate-700"}`}><span className="flex h-8 w-8 items-center justify-center rounded-lg text-xs font-bold" style={{backgroundColor:`${platform.color}20`,color:platform.color}}>{platform.shortName}</span><span className="min-w-0 truncate text-xs font-medium text-slate-200">{platform.name}</span>{active&&<Check size={13} className="absolute right-2 top-2 text-cyan-300"/>}</button>})}</div></section>}
