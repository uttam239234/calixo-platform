import { composerPlatforms } from "@/features/social/composer-config";
import type { ComposerPlatform } from "@/features/social/composer-types";
export function CharacterCounter({count,platforms}:{count:number;platforms:ComposerPlatform[]}){const selected=composerPlatforms.filter(item=>platforms.includes(item.name));const limit=selected.length?Math.min(...selected.map(item=>item.limit)):null;return <div className="flex flex-wrap items-center gap-2 text-[11px] text-muted-foreground"><span className={limit&&count>limit?"text-destructive":""}>{count}{limit?` / ${limit}`:" characters"}</span>{selected.map(item=><span key={item.name} className={count>item.limit?"text-destructive":"text-muted-foreground"}>{item.shortName}: {item.limit}</span>)}</div>}
