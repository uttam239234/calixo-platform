"use client";
import { Eye, Heart, MessageCircle, Users } from "lucide-react";
import Card from "@/components/dashboard/common/Card";
import { useSocial } from "@/features/social/SocialProvider";
const compact=(value:number)=>new Intl.NumberFormat("en-US",{notation:"compact",maximumFractionDigits:1}).format(value);
export function SocialKpiGrid(){const{accounts,posts}=useSocial();const items=[{label:"Total followers",value:compact(accounts.reduce((sum,x)=>sum+x.followers,0)),change:"+8.4%",icon:Users},{label:"Total reach",value:compact(accounts.reduce((sum,x)=>sum+x.reach,0)),change:"+14.2%",icon:Eye},{label:"Engagements",value:compact(posts.reduce((sum,x)=>sum+x.likes+x.comments+x.shares,0)),change:"+11.7%",icon:Heart},{label:"Published posts",value:String(posts.filter(x=>x.status==="Published").length),change:"This month",icon:MessageCircle}];return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(({label,value,change,icon:Icon})=><Card key={label} className="p-5"><div className="flex items-center justify-between"><span className="text-sm text-slate-400">{label}</span><span className="rounded-xl bg-slate-800 p-2 text-cyan-300"><Icon size={17}/></span></div><p className="mt-3 text-2xl font-semibold text-white">{value}</p><p className="mt-1 text-xs text-emerald-400">{change}<span className="text-slate-500"> vs last month</span></p></Card>)}</div>}
