/**
 * Demo-mode fallbacks for Ads Manager, mirroring `AnalyticsTenantDefaults.ts`.
 * Defined independently per module (not cross-imported) to avoid circular dependencies.
 */
export const ADS_ORGANIZATION_ID = "org-current";
export const ADS_CURRENT_USER_ID = "user-current";
