/** Calixo Platform — AI Copilot tenant fallback constants (used when no session user/org is present). */
export const COPILOT_ORGANIZATION_ID = "org-current";
export const COPILOT_CURRENT_USER_ID = "user-current";
export const COPILOT_WORKSPACE_ID = "workspace-growth-engine";
