/** Calixo Platform — Content Studio tenant fallback constants (used when no session user/org is present). */
export const CONTENT_ORGANIZATION_ID = "org-current";
export const CONTENT_CURRENT_USER_ID = "user-current";
