/**
 * Demo-mode fallbacks for Social Media, mirroring `AdsTenantDefaults.ts`.
 * Defined independently per module (not cross-imported) to avoid circular dependencies.
 */
export const SOCIAL_ORGANIZATION_ID = "org-current";
export const SOCIAL_CURRENT_USER_ID = "user-current";
