/** Calixo Platform — Reports tenant fallback constants (used when no session user/org is present), mirroring every sibling module's own `tenant/` file. */
export const REPORTS_ORGANIZATION_ID = "org-current";
export const REPORTS_WORKSPACE_ID = "workspace-growth-engine";
export const REPORTS_CURRENT_USER_ID = "user-current";
